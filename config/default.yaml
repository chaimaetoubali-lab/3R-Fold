seed: 42

autoencoder:
  latent_dim: 64

retrieval:
  top_k: 7

training:
  epochs: 60
  lr: 0.0001
  lambda1: 0.001
  lambda2: 0.001

dataset:
  family_threshold: 200

folding:
  bonus: 1.5
  min_pair_freq: 0.4
  unpaired_bonus: 0.3

  